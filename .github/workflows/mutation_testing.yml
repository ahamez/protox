name: Mutation testing

env:
  PROTOX_PROTOBUF_VERSION: "34.1"
  # Published by upstream in the pinned v34.1 release metadata for this asset.
  PROTOX_PROTOBUF_SHA256: "af27ea66cd26938fe48587804ca7d4817457a08350021a1c6e23a27ccc8c6904"
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

on:
  push:
    branches: ["**"]

jobs:
  build:
    runs-on: ubuntu-24.04
    name: OTP ${{matrix.otp}} / Elixir ${{matrix.elixir}}
    strategy:
      fail-fast: false
      matrix:
        include:
          - elixir: "1.17"
            otp: "27"

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 30

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{matrix.elixir}}
          otp-version: ${{matrix.otp}}

      - name: Restore dependencies cache
        uses: actions/cache@v5
        with:
          path: deps
          key: ${{ runner.os }}-${{ matrix.otp }}-${{ matrix.elixir }}-mix-${{ hashFiles('**/mix.lock') }}

      - name: Install protoc
        run: |
          protoc_zip="protoc-${{ env.PROTOX_PROTOBUF_VERSION }}-linux-x86_64.zip"
          wget "https://github.com/protocolbuffers/protobuf/releases/download/v${{ env.PROTOX_PROTOBUF_VERSION }}/${protoc_zip}"
          if ! echo "${{ env.PROTOX_PROTOBUF_SHA256 }}  ${protoc_zip}" | sha256sum --check --; then
            echo "Checksum verification failed for ${protoc_zip}" >&2
            exit 1
          fi
          unzip -d protoc "${protoc_zip}"
          echo "${PWD}/protoc/bin" >> "$GITHUB_PATH"
          "${PWD}/protoc/bin/protoc" --version

      - name: Run mutation testing
        env:
          MIX_ENV: test
        run: |
          mix local.rebar --force
          mix local.hex --force
          mix deps.get
          CHANGED_FILES="$(git diff --name-only HEAD~25..HEAD | grep '^lib/.*\.ex$' || true)"

          if [ -z "$CHANGED_FILES" ]; then
            echo "No changed Elixir source files under lib/, skipping mutation testing."
            exit 0
          fi

          status=0

          while IFS= read -r file; do
            [ -n "$file" ] || continue
            echo "Running mutation testing for $file"

            output_file="$(mktemp)"

            if mix muex --files "$file" --mutators arithmetic,comparison,boolean,conditional --fail-at 20 --timeout 30000 --optimize --optimize-level conservative --verbose >"$output_file" 2>&1; then
              cat "$output_file"
            else
              cat "$output_file"

              if grep -q "Original mutations: 0" "$output_file"; then
                echo "No mutations generated for $file, skipping threshold enforcement."
              else
                status=1
              fi
            fi
          done <<EOF
          $CHANGED_FILES
          EOF

          exit "$status"
